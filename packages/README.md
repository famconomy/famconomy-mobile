# Shared Packages

This directory will host shared TypeScript libraries that are consumed by the web, mobile, and backend applications as the cross-platform roadmap progresses. Use it to publish reusable models, hooks, and utilities once they are extracted from the existing projects.
